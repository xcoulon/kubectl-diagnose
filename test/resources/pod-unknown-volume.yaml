apiVersion: v1
kind: Pod
metadata:
  name: unknown-volume
spec:
  containers:
  - name: default
    image: caddy:2
    ports:
    - containerPort: 80
      name: http
      protocol: TCP
    resources:
      limits:
        cpu: 500m
        memory: 100Mi
      requests:
        cpu: 100m
        memory: 20Mi
    volumeMounts:
    - mountPath: "/tmp"
      name: unknown-volume
  serviceAccount: default
  serviceAccountName: default
  volumes:
  - name: unknown-volume
    hostPath:
      path: /any/path